# 星韵 (Xingyun) - 智能文档处理平台

基于 Vue.js + Flask 的 AI 驱动文档生成与编辑全栈应用。

## 项目概述

星韵是一款集自然语言理解、内容生成、多轮交互与图像处理于一体的智能文档平台，旨在提供高效、精准且灵活的文档与图片生成解决方案。

## 技术栈

### 前端

| 技术         | 说明                   |
| ------------ | ---------------------- |
| Vue.js 3     | 渐进式 JavaScript 框架 |
| Vite         | 下一代前端构建工具     |
| TipTap       | 富文本编辑器           |
| Pinia        | 状态管理               |
| TailwindCSS  | CSS 框架               |
| Axios        | HTTP 客户端            |
| Lucide Icons | 图标库                 |

### 后端

| 技术               | 说明             |
| ------------------ | ---------------- |
| Flask 3.0          | Python Web 框架  |
| Flask-SQLAlchemy   | ORM 数据库操作   |
| Flask-JWT-Extended | JWT 身份认证     |
| Flask-CORS         | 跨域资源共享     |
| PyMySQL            | MySQL 数据库驱动 |

## 项目结构

```
Xingyun/
├── frontend/                 # 前端项目
│   ├── src/
│   │   ├── components/       # Vue 组件
│   │   ├── views/            # 页面视图
│   │   ├── stores/           # Pinia 状态管理
│   │   ├── services/         # 服务层
│   │   ├── App.vue           # 根组件
│   │   ├── main.js           # Vue 入口
│   │   └── style.css         # 全局样式
│   ├── package.json
│   └── vite.config.js
│
├── backend/                  # 后端项目
│   ├── models/               # 数据模型
│   │   ├── __init__.py       # 数据库初始化
│   │   ├── user.py           # 用户模型
│   │   ├── project.py        # 项目模型
│   │   └── resource.py       # 资源模型
│   ├── routes/               # API 路由
│   │   ├── __init__.py       # 蓝图注册
│   │   ├── auth.py           # 认证接口
│   │   ├── projects.py       # 项目接口
│   │   └── resources.py      # 资源接口
│   ├── uploads/              # 上传文件存储
│   ├── app.py                # Flask 应用入口
│   ├── config.py             # 配置文件
│   └── requirements.txt      # Python 依赖
│
└── doc/                      # 项目文档
    └── 已经对接的API开发表.md
```

## 快速开始

### 环境要求

- **Node.js** >= 18.0
- **Python** >= 3.9
- **MySQL** >= 8.0

### 1. 克隆项目

```bash
git clone https://github.com/yourusername/xingyun.git
cd xingyun
```

### 2. 启动后端

```bash
# 进入后端目录
cd backend

# 创建虚拟环境（可选）
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/Mac

# 安装依赖
pip install -r requirements.txt

# 配置数据库（修改 config.py 中的数据库连接）

# 启动服务
python app.py
```

后端服务将在 `http://localhost:5000` 运行。

### 3. 启动前端

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

前端服务将在 `http://localhost:5173` 运行。

## API 接口

### 认证模块 `/api/auth`

| 方法 | 路径          | 说明             | 状态      |
| ---- | ------------- | ---------------- | --------- |
| POST | `/register` | 用户注册         | ✅ 已对接 |
| POST | `/login`    | 用户登录         | ✅ 已对接 |
| GET  | `/me`       | 获取当前用户信息 | ⏳ 待对接 |

### 项目模块 `/api/projects`

| 方法   | 路径      | 说明         | 状态      |
| ------ | --------- | ------------ | --------- |
| GET    | `/`     | 获取项目列表 | ⏳ 待对接 |
| POST   | `/`     | 创建项目     | ⏳ 待对接 |
| GET    | `/<id>` | 获取项目详情 | ⏳ 待对接 |
| PUT    | `/<id>` | 更新项目     | ⏳ 待对接 |
| DELETE | `/<id>` | 删除项目     | ⏳ 待对接 |

### 资源模块 `/api/resources`

| 方法   | 路径        | 说明     | 状态      |
| ------ | ----------- | -------- | --------- |
| POST   | `/upload` | 上传资源 | ⏳ 待对接 |
| GET    | `/<id>`   | 获取资源 | ⏳ 待对接 |
| DELETE | `/<id>`   | 删除资源 | ⏳ 待对接 |

> 详细 API 文档请参考 [已对接的API开发表](doc/已经对接的API开发表.md)

## 核心功能

### ✅ 已实现

1. **用户认证系统**

   - 用户注册/登录
   - JWT Token 身份验证
   - 用户个人中心
2. **TipTap 富文本编辑器**

   - 撤销/重做功能
   - 标题、粗体、斜体、下划线等格式
   - 有序/无序列表
   - 插入图片和表格
3. **材料管理**

   - 上传参考材料（DOCX、PDF、TXT、MD、HTML）
   - 材料列表展示和删除
4. **AI 对话界面**

   - 与 AI 助手实时对话
   - 消息历史记录

### 🚧 待实现

1. **AI 功能集成**

   - 基于材料的智能内容生成
   - 大纲自动生成
   - 内容优化和润色
2. **文档导出**

   - 导出为 Word（.docx）
   - 导出为 PDF
   - 导出为 LaTeX / Markdown
3. **图表生成**

   - 流程图（Mermaid.js）
   - 架构图（PlantUML）
   - AI 生成图片

## 开发指南

### 前端开发

```bash
cd frontend

# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

### 后端开发

```bash
cd backend

# 启动开发服务器（带热重载）
python app.py

# 数据库迁移（如需要）
flask db upgrade
```

## 配置说明

### 前端配置

编辑 `frontend/src/stores/auth.js`：

```javascript
// API 基础 URL
const API_BASE_URL = 'http://localhost:5000/api'

// 开发模式（设为 false 连接真实后端）
const DEV_MODE = false
```

### 后端配置

编辑 `backend/config.py`：

```python
# 数据库配置
SQLALCHEMY_DATABASE_URI = 'mysql+pymysql://user:password@localhost/xingyun'

# JWT 密钥
JWT_SECRET_KEY = 'your-secret-key'
```

## 贡献指南

1. Fork 本项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 许可证

MIT License

## 联系方式

项目链接: [https://github.com/yourusername/xingyun](https://github.com/yourusername/xingyun)
